legacy-peer-deps=true

# Prevent npm from generating package-lock.json; this repo uses pnpm-lock.yaml.
package-lock=false

# 允许运行所有包的构建脚本
# esbuild 和 vue-demi 是已知安全的包，需要 postinstall 脚本才能正常工作
ignore-scripts=false
